name: Deploy to OCI

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main
  workflow_dispatch:
    inputs:
      skip_tests:
        description: "Skip tests and build"
        required: false
        type: boolean
        default: false

env:
  NODE_ENV: test
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: "true"

jobs:
  static:
    if: github.event.inputs.skip_tests != 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Lint
        run: pnpm lint

      - name: Typecheck
        run: pnpm typecheck

  test:
    if: github.event.inputs.skip_tests != 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        run: pnpm install

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        id: playwright-cache
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright browsers
        if: steps.playwright-cache.outputs.cache-hit != 'true'
        env:
          DEBUG: "pw:install"
        run: pnpm --filter @ghostmail/client exec playwright install --with-deps chromium firefox

      - name: Install Playwright system dependencies
        if: steps.playwright-cache.outputs.cache-hit == 'true'
        run:
          pnpm --filter @ghostmail/client exec playwright install-deps chromium
          firefox

      - name: Run tests
        run: pnpm test

  build-and-push:
    needs: [static, test]
    if: |
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch') &&
      github.event.inputs.skip_tests != 'true'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    outputs:
      client: ${{ steps.filter.outputs.client }}
      smtp: ${{ steps.filter.outputs.smtp }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Check path changes
        uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            client:
              - 'packages/client/**'
              - 'packages/database/**'
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'pnpm-workspace.yaml'
              - 'packages/client/Dockerfile'
              - '.github/workflows/deploy.yml'
            smtp:
              - 'packages/smtp-server/**'
              - 'packages/database/**'
              - 'package.json'
              - 'pnpm-lock.yaml'
              - 'pnpm-workspace.yaml'
              - 'packages/smtp-server/Dockerfile'
              - '.github/workflows/deploy.yml'

      - name: Setup pnpm
        if: steps.filter.outputs.client == 'true' || steps.filter.outputs.smtp == 'true' || github.event_name == 'workflow_dispatch'
        uses: pnpm/action-setup@v4

      - name: Setup Node
        if: steps.filter.outputs.client == 'true' || steps.filter.outputs.smtp == 'true' || github.event_name == 'workflow_dispatch'
        uses: actions/setup-node@v4
        with:
          node-version: 24
          cache: "pnpm"

      - name: Install dependencies
        if: steps.filter.outputs.client == 'true' || steps.filter.outputs.smtp == 'true' || github.event_name == 'workflow_dispatch'
        run: pnpm install --frozen-lockfile

      - name: Build packages
        if: steps.filter.outputs.client == 'true' || steps.filter.outputs.smtp == 'true' || github.event_name == 'workflow_dispatch'
        run: pnpm build
        env:
          VITE_SMTP_HOST: smtp.ghostmail.dev
          VITE_SMTP_PORT: 587
          VITE_MAIL_DOMAIN: ghostmail.dev

      - name: Log in to GitHub Container Registry
        if: steps.filter.outputs.client == 'true' || steps.filter.outputs.smtp == 'true' || github.event_name == 'workflow_dispatch'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up QEMU
        if: steps.filter.outputs.client == 'true' || steps.filter.outputs.smtp == 'true' || github.event_name == 'workflow_dispatch'
        uses: docker/setup-qemu-action@v3

      - name: Set up Docker Buildx
        if: steps.filter.outputs.client == 'true' || steps.filter.outputs.smtp == 'true' || github.event_name == 'workflow_dispatch'
        uses: docker/setup-buildx-action@v3

      - name: Build and push Client image
        if: steps.filter.outputs.client == 'true' || github.event_name == 'workflow_dispatch'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: packages/client/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/ghostmail-client:latest
          platforms: linux/arm64,linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Build and push SMTP image
        if: steps.filter.outputs.smtp == 'true' || github.event_name == 'workflow_dispatch'
        uses: docker/build-push-action@v5
        with:
          context: .
          file: packages/smtp-server/Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/ghostmail-smtp:latest
          platforms: linux/arm64,linux/amd64
          cache-from: type=gha
          cache-to: type=gha,mode=max

  deploy:
    needs: [static, test, build-and-push]
    if: |
      always() &&
      (github.event_name == 'push' || github.event_name == 'workflow_dispatch') &&
      (needs.static.result == 'success' || needs.static.result == 'skipped') &&
      (needs.test.result == 'success' || needs.test.result == 'skipped') &&
      (needs.build-and-push.result == 'success' || needs.build-and-push.result == 'skipped') &&
      (needs.build-and-push.outputs.client == 'true' || needs.build-and-push.outputs.smtp == 'true' || github.event_name == 'workflow_dispatch')
    runs-on: ubuntu-latest
    environment: Deploy
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Copy docker-compose.yml to server
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          source: "docker-compose.yml"
          target: "~/ghostmail"

      - name: Create .env file on server
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          script: |
            mkdir -p ~/ghostmail
            echo "MONGO_USER=${{ secrets.MONGO_USER }}" > ~/ghostmail/.env
            echo "MONGO_PASSWORD=${{ secrets.MONGO_PASSWORD }}" >> ~/ghostmail/.env
            echo "SESSION_SECRET=${{ secrets.SESSION_SECRET }}" >> ~/ghostmail/.env
            echo "GITHUB_REPOSITORY_OWNER=${{ github.repository_owner }}" >> ~/ghostmail/.env
            echo "DEPLOY_USER=${{ secrets.DEPLOY_USER }}" >> ~/ghostmail/.env

      - name: SSH into OCI and Deploy
        uses: appleboy/ssh-action@v1.0.3
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        with:
          host: ${{ secrets.DEPLOY_HOST }}
          username: ${{ secrets.DEPLOY_USER }}
          key: ${{ secrets.DEPLOY_SSH_KEY }}
          envs: GITHUB_TOKEN
          script: |
            mkdir -p ~/ghostmail
            cd ~/ghostmail
            # Log in to GHCR (suppress standard unencrypted credentials warning unless it fails)
            LOGIN_ERR=$(mktemp)
            if ! echo "$GITHUB_TOKEN" | docker login ghcr.io -u "${{ github.actor }}" --password-stdin 2>"$LOGIN_ERR"; then
              cat "$LOGIN_ERR" >&2
              rm -f "$LOGIN_ERR"
              exit 1
            fi
            rm -f "$LOGIN_ERR"
            # Pull latest images from GHCR
            docker compose -p ghostmail pull
            # Restart services
            docker compose -p ghostmail up -d --remove-orphans
            # Cleanup old images to save disk space
            docker image prune -f
